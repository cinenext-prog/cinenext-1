# 🔧 GitHub Pages 部署 - 404 问题修复

## ❌ 问题诊断

你遇到的 404 错误可能有以下原因：

1. **GitHub Secret 未设置** ⚠️ 最常见的原因
2. **GitHub Pages 未启用或配置不正确**
3. **工作流构建失败** (已修复)

---

## ✅ 修复步骤

### 步骤 1️⃣：添加 LIVEPEER_API_KEY Secret（重要！）

1. **打开仓库设置**：
   https://github.com/cinenext-prog/cinenext-1/settings/secrets/actions

2. **点击 "New repository secret"**

3. **填写信息**：
   - **Name**: `LIVEPEER_API_KEY`
   - **Secret**: `a3eaad66-77f7-4e15-8ee7-4ee59865e603`

4. **点击 "Add secret"**

---

### 步骤 2️⃣：检查 GitHub Pages 设置

1. **打开仓库设置**：
   https://github.com/cinenext-prog/cinenext-1/settings/pages

2. **确保以下配置**：
   - **Source**: 选择 "Deploy from a branch"
   - **Branch**: 选择 `gh-pages` 
   - **Folder**: 选择 `/ (root)`

3. **如果 gh-pages 分支不存在**：
   - 必须先成功运行一次部署，GitHub 会自动创建
   - 或者手动创建一个空的 `gh-pages` 分支

---

### 步骤 3️⃣：查看部署状态

1. **打开 Actions 标签页**：
   https://github.com/cinenext-prog/cinenext-1/actions

2. **查看最新的工作流运行**：
   - 应该看到 "fix: configure git user..." 的运行记录
   - 检查状态是否为 ✅ 成功

3. **如果仍然失败**：
   - 点击工作流查看详细日志
   - 查找错误信息

---

## 🔄 强制重新部署

如果设置好 Secret 后部署仍未成功，可以强制重新部署：

### 方法 1：推送空提交
```bash
cd /workspaces/cinenext-1
git commit --allow-empty -m "trigger: force redeploy"
git push origin main
```

### 方法 2：在 GitHub UI 中重新运行
1. 打开 Actions 标签页
2. 点击最后一个失败的工作流
3. 点击 "Re-run jobs" → "Re-run all jobs"

---

## 🌐 验证部署

### 检查清单

部署成功后，按顺序检查：

- [ ] **GitHub Actions** - ✅ 最新工作流显示为成功
- [ ] **gh-pages 分支** - ✅ 存在于仓库中
- [ ] **GitHub Pages 设置** - ✅ 显示 "Your site is published at..."
- [ ] **访问应用** - ✅ https://cinenext-prog.github.io/cinenext-1/ 可以打开

### 访问应用

如果一切配置正确，应用将在：
```
https://cinenext-prog.github.io/cinenext-1/
```

---

## 🆘 调试技巧

### 如何查看构建日志

1. 打开 Actions 标签页
2. 点击最新的工作流运行
3. 点击 "build-and-deploy" 任务
4. 展开各步骤查看日志

### 常见错误信息及解决方案

**错误：`Error: The process '/usr/bin/git' failed`**
- ✅ 已修复！添加了 Git 用户配置
- 重新运行部署

**错误：`LIVEPEER_API_KEY is null`**
- ❌ Secret 未设置或配置不正确
- 检查 Settings → Secrets → 确认 LIVEPEER_API_KEY 存在

**错误：`Resource not found`**
- ❌ GitHub Pages 未启用
- 检查 Settings → Pages → 确保启用并指向 gh-pages 分支

---

## 📋 快速检查清单

执行以下命令验证本地设置：

```bash
# 检查 vite.config.js
cat vite.config.js | grep base

# 检查 package.json 中的 deploy 脚本
cat package.json | grep -A 1 '"deploy"'

# 检查 .env 文件
cat .env

# 检查工作流文件
cat .github/workflows/deploy.yml | grep -A 5 "LIVEPEER_API_KEY"
```

---

## 🎯 预期的部署流程

```
你修改代码
    ↓
git push origin main
    ↓
GitHub Actions 自动运行
    ↓
构建项目
    ↓
部署到 gh-pages 分支
    ↓
GitHub Pages 更新
    ↓
应用实时生效 (可能需要 1-2 分钟)
```

---

## 💡 提示

- **首次部署可能需要 5-10 分钟**，因为 GitHub 需要创建 gh-pages 分支
- **浏览器缓存**：如果看到旧版本，按 `Ctrl+Shift+Delete` 清除缓存
- **HTTPS**：所有 GitHub Pages 都是 HTTPS，不用担心安全问题

---

## ✨ 完整的工作流已准备就绪！

现在只需：
1. ✅ 添加 LIVEPEER_API_KEY secret
2. ✅ 检查 GitHub Pages 设置
3. ✅ 等待部署完成
4. ✅ 访问你的应用！

有任何问题都可以告诉我！🚀
