# 🔧 修复 GitHub Pages 404 错误

## ⚠️ 问题诊断

你的 GitHub Pages 当前配置有问题：
- ❌ 源分支设置为 `main`（需要改为 `gh-pages`）
- ✅ 但我们已经部署了文件到 `gh-pages` 分支

## 🔧 解决方案：在 GitHub 网站上配置

### 步骤 1️⃣：打开 GitHub Pages 设置

**访问这个链接**：
```
https://github.com/cinenext-prog/cinenext-1/settings/pages
```

或者手动操作：
1. 打开 https://github.com/cinenext-prog/cinenext-1
2. 点击 **Settings**（设置）
3. 左侧菜单点击 **Pages**

---

### 步骤 2️⃣：修改 Source（源）配置

在 **Source** 部分，你会看到两个下拉菜单：

**当前状态**:
```
Branch: main
Folder: / (root)
```

**需要改为**:
```
Branch: gh-pages    ← 改这里！
Folder: / (root)
```

具体操作：

1. **点击 "Branch" 下拉菜单**（显示 "main"）
2. **选择 "gh-pages"**
3. **确保 Folder 是 "/ (root)"**
4. **点击 Save**（保存）

---

### 步骤 3️⃣：等待部署

保存后，GitHub 会：
1. 开始构建 (显示 "Building")
2. 部署文件 (显示 "Active")

完成后箭头会变绿 ✅，你会看到：
```
Your site is live at https://cinenext-prog.github.io/cinenext-1/
```

---

## 📸 截图参考

大致区域应该是这样的：
```
┌─────────────────────────────────────────────────────┐
│ GitHub Pages                                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│ Source                                              │
│  Branch:   [ main      ▼ ] ← 点这里改为 gh-pages  │
│  Folder:   [ / (root) ▼ ]                           │
│                                                     │
│ [Save] 按钮 ← 点这个保存                            │
│                                                     │
│ Your site is live at:                               │
│ https://cinenext-prog.github.io/cinenext-1/         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## ✅ 完成后

1. 等待 1-2 分钟
2. 刷新浏览器访问：
   ```
   https://cinenext-prog.github.io/cinenext-1/
   ```
3. 应该能看到你的 Livepeer 视频播放器应用！

---

## 🆘 如果仍然失败

如果修改后仍然 404：

1. **清除浏览器缓存**：
   - 按 `Ctrl+Shift+Delete` (Windows) 或 `Cmd+Shift+Delete` (Mac)
   - 或者用无痕模式重新打开

2. **检查分支内容**：
   - 在 GitHub 上查看 gh-pages 分支
   - 确保有 `dist/index.html` 文件

3. **等待更长时间**：
   - 有时需要 5-10 分钟才能完全更新

---

## 💡 快速链接

- **设置页面**: https://github.com/cinenext-prog/cinenext-1/settings/pages
- **应用 URL**: https://cinenext-prog.github.io/cinenext-1/
- **gh-pages 分支**: https://github.com/cinenext-prog/cinenext-1/tree/gh-pages

---

**现在就去修改设置，你的应用应该就能正常访问了！** 🚀
